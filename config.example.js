// Supabase cloud sync configuration.
// It is safe for a public GitHub Pages frontend to contain the anon/publishable key,
// provided Row Level Security (RLS) is enabled. NEVER put service_role here.
window.STUDY_APP_CONFIG = {
  supabaseUrl: "YOUR_SUPABASE_PROJECT_URL",
  supabaseKey: "YOUR_SUPABASE_ANON_OR_PUBLISHABLE_KEY"
};
